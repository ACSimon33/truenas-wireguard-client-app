# Set default behavior to automatically normalize line endings
* text=auto

# Shell scripts must use Unix line endings (LF)
*.sh text eol=lf

# Use Unix line endings for YAML files
*.yml text eol=lf
*.yaml text eol=lf
